# Godot 4+ specific ignores
.godot/
/android/
export/
*.translation

# Web export output
build/web/

# Wrangler local state
.wrangler/

# Full Kenney asset library (local reference — copy into assets/ for commits)
Kenney/

# Dev-only (paid Godot MCP Pro — install locally from your zip)
addons/godot_mcp/

# Local Cursor MCP config (paths are machine-specific)
.cursor/mcp.json
.preview/

# Local Claude Code state (skills symlink is machine-specific)
.claude/

# Local Cloudflare credentials/config (analytics worker)
analytics-worker/.env
.wrangler/

# Cloudflare Pages headers (copied into build/web/ on deploy).
# For threaded export, also add COOP/COEP on /* — see docs/DEPLOYMENT.md.

/*.wasm.gz
  Content-Type: application/wasm
  Content-Encoding: gzip
